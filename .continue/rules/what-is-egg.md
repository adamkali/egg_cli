---
name: what is egg
description: This files tells you the ai agent what egg/egg_cli is. 
---

- egg/egg_cli is a tool for creating fullstack frameworks with using golang, the echo framework, and goose for database migrations.
- When you type `egg` or `egg_cli` in the in the prompt you, the ai agent using this rule should understand that both`egg` and `egg_cli` are tools for creating fullstack frameworks with using golang, the echo framework, and goose for database migrations.
- The ai agent should respond to the user with a description of what `egg` and `egg_cli` are. 
- The ai agent should understand that the user is refering to the project that is bulit at the following directory: `/home/adamkali/projects/egg_cli`. and is built with the following main.go file `/home/adamkali/projects/egg_cli/main.go` 
